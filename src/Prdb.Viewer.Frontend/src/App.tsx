export function App() {
  return (
    <main className="shell">
      <section className="hero" aria-labelledby="welcome-title">
        <span className="eyebrow">Your library, ready when you are</span>
        <h1 id="welcome-title">prdb-viewer</h1>
        <p>
          The application is running. Guided setup and the first playable Video
          arrive in the next implementation slice.
        </p>
        <div className="status" role="status">
          <span aria-hidden="true" />
          Walking Skeleton online
        </div>
      </section>
    </main>
  )
}
